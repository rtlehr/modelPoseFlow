import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.modelposeflow',
  appName: 'modelposeflow-app',
  webDir: 'www'
};

export default config;
